"""
Simulation Logic — Self-contained cashflow simulation module.

Runs independently from the main server/ code on a separate port.
Generates smaller, interpretable numbers and runs the full simulation
window in one shot, producing day-by-day logs.

Usage:
    python -m simulation_logic.app
"""
